{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in *.sh) command -v shfmt >/dev/null && shfmt -i 2 -w \"$f\"; shellcheck \"$f\" >&2 || exit 2;; esac",
            "timeout": 30,
            "statusMessage": "shfmt + shellcheck"
          }
        ]
      }
    ]
  }
}
